# EnterpriseAgent 服务器部署操作手册

## 📦 一、准备工作

### 1.1 上传压缩包到服务器

```bash
# 使用 SCP 上传
scp enterprise-agent-final.tar.gz root@your-server:/opt/

# 或使用 FTP/SFTP 工具（WinSCP、FileZilla）上传到 /opt/ 目录
```

### 1.2 登录服务器

```bash
ssh root@your-server
```

---

## 🚀 二、首次部署

### 2.1 解压项目

```bash
cd /opt
mkdir enterprise-agent
cd enterprise-agent
tar -xzf ../enterprise-agent-final.tar.gz
```

### 2.2 配置环境变量

```bash
vim .env
```

**修改以下配置（必填）：**

```bash
# LLM 配置
LLM_API_KEY=你的API密钥
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4

# 可选修改
ADMIN_PASSWORD=你的管理员密码
LOG_LEVEL=INFO
```

**保存并退出**：按 `ESC`，输入 `:wq`，回车

### 2.3 创建数据目录

```bash
mkdir -p data data/knowledge .cache
chmod -R 777 data .cache
```

### 2.4 构建镜像

```bash
#1. 先清除缓存
df -h                          # 看空间
docker image prune -f          # 清悬空镜像(运行中也安全)
docker builder prune -f        # 清构建缓存
# 2. 重新构建镜像（
docker compose down
docker compose build --no-cache

# 3. 启动容器
docker compose up -d

# 重启（无需重新构建镜像）
docker compose restart

```

**等待构建完成（约 15-20 分钟）**

### 2.5 启动服务

```bash
docker compose up -d
```

### 2.6 查看日志（确认启动成功）

```bash
docker compose logs -f
```

**成功的日志标志：**
```
✅ 数据库表检查完成
✅ 启动服务: 0.0.0.0:8802
✅ 引擎模式: langgraph
✅ INFO: Uvicorn running on http://0.0.0.0:8802
```

按 `Ctrl+C` 退出日志查看

### 2.7 初始化示例数据（可选）

```bash
docker exec -it enterprise-agent python seed.py
```

**说明**：此步骤会创建示例知识库、客户、订单数据，用于测试功能。不执行也可正常使用。

---

## 🌐 三、访问服务

- **用户聊天页**: http://服务器IP:8802
- **后台管理页**: http://服务器IP:8802/admin
- **知识库管理**: http://服务器IP:8802/kb

---

## 🔄 四、更新部署（有新版本时）

### 4.1 停止服务

```bash
cd /opt/enterprise-agent
docker compose down
```

### 4.2 备份配置和数据

```bash
cp .env .env.backup
tar -czf data-backup-$(date +%Y%m%d).tar.gz data/
```

### 4.3 上传新版本压缩包

```bash
# 上传 enterprise-agent-final.tar.gz 到 /opt/
```

### 4.4 解压覆盖

```bash
cd /opt/enterprise-agent
tar -xzf ../enterprise-agent-final.tar.gz

# 恢复配置文件
cp .env.backup .env
```

### 4.5 重新构建并启动

```bash
docker compose build
docker compose up -d
docker compose logs -f
```

---

## 🔧 五、常用管理命令

### 5.1 查看服务状态

```bash
docker compose ps
```

### 5.2 查看实时日志

```bash
docker compose logs -f
```

### 5.3 重启服务

```bash
docker compose restart
```

### 5.4 停止服务

```bash
docker compose down
```

### 5.5 进入容器

```bash
docker exec -it enterprise-agent bash
```

### 5.6 查看容器资源使用

```bash
docker stats enterprise-agent
```

### 5.7 清理旧镜像（释放磁盘空间）

```bash
docker system prune -a
```

---

## 📊 六、数据管理

### 6.1 备份数据

```bash
cd /opt/enterprise-agent
tar -czf backup-$(date +%Y%m%d-%H%M%S).tar.gz data/
```

### 6.2 恢复数据

```bash
cd /opt/enterprise-agent
docker compose down
tar -xzf backup-20260605-100000.tar.gz
docker compose up -d
```

### 6.3 查看数据库统计

```bash
docker exec -it enterprise-agent python -c "
from database.connection import get_db_connection
conn = get_db_connection()
print('会话数:', conn.execute('SELECT COUNT(*) FROM conversation_sessions').fetchone()[0])
print('对话数:', conn.execute('SELECT COUNT(*) FROM conversations').fetchone()[0])
print('文档数:', conn.execute('SELECT COUNT(*) FROM documents').fetchone()[0])
conn.close()
"
```

---

## 🐛 七、故障排查

### 7.1 服务无法启动

```bash
# 查看详细日志
docker compose logs -f

# 检查端口占用
netstat -tulpn | grep 8802

# 检查磁盘空间
df -h

# 检查内存
free -h
```

### 7.2 数据库错误

```bash
# 检查数据目录权限
ls -la /opt/enterprise-agent/data

# 修复权限
chmod -R 777 /opt/enterprise-agent/data

# 重启服务
docker compose restart
```

### 7.3 模型下载失败

```bash
# 检查网络连接
curl -I https://hf-mirror.com

# 手动进入容器下载
docker exec -it enterprise-agent bash
python -c "from sentence_transformers import SentenceTransformer; SentenceTransformer('BAAI/bge-small-zh-v1.5')"
exit
```

### 7.4 API Key 配置错误

**方式 1：修改 .env 文件**
```bash
vim .env
# 修改 LLM_API_KEY
docker compose restart
```

**方式 2：后台管理页面修改**
- 访问 http://服务器IP:8802/admin
- 点击"模型设置"
- 修改并保存（立即生效，无需重启）

---

## 🔒 八、安全配置

### 8.1 修改管理员密码

```bash
vim .env
# 修改 ADMIN_PASSWORD=新密码
docker compose restart
```

### 8.2 配置防火墙

```bash
# 开放 8802 端口
ufw allow 8802/tcp
ufw reload
```

### 8.3 配置 Nginx 反向代理（可选）

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://localhost:8802;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

### 8.4 配置 HTTPS（推荐）

```bash
# 安装 certbot
apt-get install certbot python3-certbot-nginx

# 获取证书
certbot --nginx -d your-domain.com

# 自动续期
certbot renew --dry-run
```

---

## 📋 九、定期维护

### 9.1 每日备份（设置 cron）

```bash
crontab -e

# 添加以下行（每天凌晨 2 点备份）
0 2 * * * cd /opt/enterprise-agent && tar -czf /backup/data-$(date +\%Y\%m\%d).tar.gz data/
```

### 9.2 每周清理日志

```bash
crontab -e

# 添加以下行（每周日凌晨 3 点清理）
0 3 * * 0 docker system prune -f
```

### 9.3 监控磁盘空间

```bash
# 查看磁盘使用
df -h

# 查看 Docker 占用
docker system df
```

---

## 📞 十、联系与支持

### 配置文件位置
- 环境变量：`/opt/enterprise-agent/.env`
- 数据库：`/opt/enterprise-agent/data/platform.db`
- 知识库：`/opt/enterprise-agent/data/knowledge/`
- 模型缓存：`/opt/enterprise-agent/.cache/`

### 日志位置
- 容器日志：`docker compose logs -f`
- 应用日志：容器内无持久化日志（通过 stdout 输出）

### 关键配置
- **端口**: 8802
- **引擎**: langgraph（固定）
- **RAG模式**: vector（固定）
- **数据持久化**: data 目录通过 volume 挂载

---

## ✅ 快速命令速查表

```bash
# 启动服务
docker compose up -d

# 停止服务
docker compose down

# 重启服务
docker compose restart

# 查看状态
docker compose ps

# 查看日志
docker compose logs -f

# 进入容器
docker exec -it enterprise-agent bash

# 备份数据
tar -czf backup-$(date +%Y%m%d).tar.gz data/

# 重新构建
docker compose build --no-cache

# 查看资源
docker stats enterprise-agent
```

---

**部署完成后，访问 http://服务器IP:8802 开始使用！** 🎉
