# ============================================================
# Python 版本要求: 3.11 或 3.12 (推荐 3.11)
# Python 3.13 暂不支持（numpy/sentence-transformers 编译问题）
# ============================================================

# 核心依赖（生产环境必需）
fastapi==0.115.0
uvicorn==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.9

# LangChain 生态
langchain-core==0.3.28
langchain==0.3.11
langchain-openai==0.2.14
langgraph==0.2.62

# 向量/NLP（RAG 功能必需）
sentence-transformers==3.3.1  # 会自动安装兼容的 numpy

# 向量索引（FAISS HNSW）
faiss-cpu==1.9.0

# 数据库
sqlalchemy==2.0.36

# 文档解析
pypdf==5.1.0
python-docx==1.1.2
pymupdf==1.24.14  # PDF 解析（import fitz）

# HTTP 客户端
httpx==0.27.2

# 浏览器自动化（本地 headed 模式，首次安装后需运行: playwright install chromium）
playwright==1.49.1

# 开发依赖
pytest==8.3.3
